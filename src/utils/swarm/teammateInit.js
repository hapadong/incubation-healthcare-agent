// Stub — teammate hook initialization removed in Phase 0 (tmux/iTerm2 backends deleted).
// HealthAgent sessions are never spawned as in-process teammates,
// so this path is never reached at runtime.
export function initializeTeammateHooks(_setAppState, _sessionId, _opts) {
  // no-op
}
